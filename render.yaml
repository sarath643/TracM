services:
  - type: web
    name: tracm
    env: docker
    buildCommand: docker build -t tracm .
    startCommand: docker run -p $PORT:3000 tracm
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
